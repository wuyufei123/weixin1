server:
  port: 8080
  servlet:
    context-path: /
spring:
  mvc:
    view:
      prefix: classpath:/static/
      suffix: .html
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/weixin?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
#mybatis配置
mybatis:
  mapper-locations: classpath*:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
